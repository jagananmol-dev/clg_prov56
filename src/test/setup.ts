/**
 * @file test/setup.ts
 * @description Global Vitest setup — runs before every test file.
 * Adds jest-dom matchers (toBeInTheDocument, toBeDisabled, etc.) to `expect`.
 */
import '@testing-library/jest-dom/vitest';
